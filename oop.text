OOP 
//data types
- reference types(class, array, delegate, event, object, interface)- heap
-value/primitive types (bool, datetime,long,short, int,interface)- stack
//pure function
function (int a, int b){return a+b;}

//impure function // dışarıdan gelecek kdv değeri değişirse methodun sonucu da değişir
var kdv=18;
function A(int a, int b) { return (a+b)* kdv}
function B(){kdv=15;}